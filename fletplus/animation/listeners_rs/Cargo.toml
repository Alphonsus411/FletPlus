[package]
name = "listeners_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "listeners_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }

[package.metadata.maturin]
name = "fletplus.animation.listeners_rs"
